[pytest]
asyncio_mode = auto
pythonpath = .

markers =
    unit: offline unit tests — no network required (default, always run)
    integration: live-network tests — opt-in via -m integration

addopts = -m "not integration"

filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnraisableExceptionWarning
